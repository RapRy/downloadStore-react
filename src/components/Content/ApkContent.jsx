import React from "react";
import { makeStyles } from "@material-ui/core";

const ApkContent = ({ content }) => {
  const classes = useStyles();
  return <div>sadasdsa</div>;
};

const useStyles = makeStyles((theme) => ({}));

export default ApkContent;
